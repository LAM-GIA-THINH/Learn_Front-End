let hasSolvedChallenge = false
let hasHintsLeft = false

// Tạo một câu lệnh if kiểm tra cả hai biến đều là false.
// Nếu đúng, chạy hàm showSolution()

function showSolution() {
    console.log("Hiển thị giải pháp....")
}

if (!hasSolvedChallenge && !hasHintsLeft) {
    showSolution()
}
