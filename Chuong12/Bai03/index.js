// Lưu một giá trị vào localStorage
// Xóa mã của bạn và làm mới trang
// Lấy giá trị của bạn từ localStorage và in nó ra console

// localStorage.setItem("myCredits", "100")
let myCredits = localStorage.getItem("myCredits")
console.log(myCredits)