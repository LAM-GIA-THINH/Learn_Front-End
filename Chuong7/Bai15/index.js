// Tạo hai biến, name và greeting
let name = "Elliot"; 
let greeting = "Hi, my name is ";

// Tạo một biến myGreeting nối hai chuỗi lại với nhau
let myGreeting = greeting + name;

// In myGreeting ra bảng điều khiển
console.log(myGreeting);
