let cards = [7, 3, 9]

// Tạo một vòng lặp for để in ra tất cả các thẻ trong mảng
// Sử dụng cards.length để chỉ định số lần vòng lặp chạy


for(i=0; i< cards.length ; i++){
    console.log(cards[i]);
}
